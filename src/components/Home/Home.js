import React from 'react';
import PropTypes from 'prop-types';
import './Home.css';
import Header from "../Header/Header";

const Home = () => (
    <Header />
);

Home.propTypes = {};

Home.defaultProps = {};

export default Home;
