import React from 'react';
import PropTypes from 'prop-types';
import './Home.css';

const Home = () => (
  <div>
    یک تست است
  </div>
);

Home.propTypes = {};

Home.defaultProps = {};

export default Home;
